Installation Instructions
=========================

(note: this doesn't work yet)
1.  cd into this directory
2.  execute "python setup.py install". you'll probably need to add sudo to
    the beginning.
